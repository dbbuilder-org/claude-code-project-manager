# Project Manager — Consolidated Roadmap

**Date:** 2026-04-12
**Author:** Chris Therriault
**Status:** Reconciled from 3 prior documents (ROADMAP.md, TODO_2026-04-09.md, SESSION_CONTEXT files)
**Repository:** https://github.com/dbbuilder-org/claude-code-project-manager
**Current state:** 465 tests, 76% coverage, all work uncommitted (see Commit section below)

---

## Document Reconciliation

| Document | Status |
|----------|--------|
| `ROADMAP.md` | Superseded by this document |
| `code-review/2026-04-09/TODO_2026-04-09.md` | 38 of 51 items complete — 13 remaining |
| `docs/SESSION_CONTEXT_2026-03-31.md` | Infrastructure only — iMessage/Cloudflare setup done |
| `docs/SESSION_CONTEXT.md` | Notes file — no open action items |

---

## Current State

### Health Scorecard

| Area | Score | Notes |
|------|-------|-------|
| Architecture | 9/10 | Clean separation, migrations versioned, filter helpers extracted |
| Security | 8/10 | `shell=True` removed, path sanitized, atomic writes — AppleScript injection still open |
| Test Coverage | 8/10 | 465 tests, 76% overall; `pm/cli.py` at 24% (CLI integration gap) |
| Infrastructure | 9/10 | CI on macos-latest, dashboard bound to 127.0.0.1, DB at `~/.pm/` |
| Agent System | 9/10 | Assess/execute/escalate complete, memory, triage, costs, scheduling |

### What's Complete (since code review 2026-04-09)

**Phase 1 — Foundation Hardening (23 SP)**
- [x] **TD-001** — Removed `shell=True` from tmux subprocess
- [x] **TD-002** — Sanitized `project.name` before filesystem use
- [x] **TD-003** — Deleted dead `launch` command (~160 lines)
- [x] **TD-004** — Thread-safe `init_db()` with `threading.Lock()`
- [x] **TD-005** — Migration wrapped in transaction; `schema_migrations` version tracking
- [x] **TD-006** — Fixed bare `except:` in `tags_list` → `except json.JSONDecodeError:`
- [x] **TD-007** — `tests/test_metadata.py` — 47 tests, `pm/metadata.py` at 97%
- [x] **TD-008** — Tests for all new CLI commands (brief, someday, urgent --all, shutdown)
- [x] **TD-010** — Narrowed migration error swallowing to `OperationalError("duplicate column")`
- [x] **TD-018** — Atomic write in `write_pm_status` via tmp + `os.replace()`

**Phase 2 — Code Quality & Infrastructure (30 SP)**
- [x] **TD-009** — Dashboard bound to `127.0.0.1`
- [x] **TD-011** — `--permission-mode` param on executor; readonly templates use `readonly`
- [x] **TD-012/013** — `sync_project_to_file()` canonical helper in `pm/metadata.py`
- [x] **TD-014** — `apply_project_filter()` replaces 6 duplicated filter blocks
- [x] **TD-015** — Deleted empty `pm/api/` stub
- [x] **TD-016** — Single `PRIORITY_LABELS` constant in `models.py`
- [x] **TD-017** — All deferred imports moved to top of `pm/cli.py`
- [x] **TD-019** — `tests/test_dashboard_helpers.py` — 20 tests
- [x] **TD-020** — `.github/workflows/test.yml` CI (macos-latest, 75% threshold)
- [x] **TD-021** — Port conflict detection in `dashboard.sh` + `PM_DASHBOARD_PORT` override
- [x] **TD-022** — DB at `~/.pm/projects.db`, `PM_DB_PATH` env override

**Phase 3 — Agent Orchestration (60+ SP)**
- [x] `pm/agent/planner.py` — assess phase (read-only, confidence 0–100, risk classification)
- [x] `pm/agent/runner.py` — execute phase (headless Claude, configurable tools/budget)
- [x] `pm/agent/coordinator.py` — ThreadPoolExecutor batch with escalation hook
- [x] `pm/agent/escalation.py` — iMessage send + chat.db reply polling (10m timeout)
- [x] `pm agent assess/run/batch` CLI commands
- [x] Dashboard Agents tab (render_agents_tab)
- [x] `agent_runs` table with versioned migration

**Phase 4 — Advanced Intelligence (built)**
- [x] `pm/agent/memory.py` — per-project `docs/AGENT-CONTEXT.md`, planner reads before assessing
- [x] `pm agent memory <project> [--clear]` — view/clear agent memory
- [x] `pm schedule install/uninstall/status` — launchd nightly agent batch
- [x] `pm triage [--imessage]` — pending decision assessment + recommendation
- [x] `pm agent costs [--days N]` — per-project run costs + $10/week budget alert
- [x] `_record_agent_run()` — writes `AgentRun` DB record after every assess+execute
- [x] `cost_usd` column on `AgentRun` (migration v2)
- [x] `pm gc [--dry-run]` — remove stale DB records for deleted projects

**Tier 1–3 Features (built earlier)**
- [x] `pm urgent / pm urgent --all` — filtered urgency view
- [x] `pm someday <project>` — park to priority 5 pile
- [x] `pm brief [--imessage] [--only-if-urgent]` — daily intelligent briefing
- [x] `pm shutdown` — iTerm2 session graceful shutdown
- [x] Dashboard inline editing for priority/deadline/target/notes/tags
- [x] `pm digest` — week-to-date activity digest
- [x] 7 document generation templates
- [x] `pm stale --action` — interactive stale project triage

---

## Remaining Backlog

### Sprint A: Quick Security + Quality Fixes (~6 SP)

These are fast, high-confidence wins that require no design decisions:

| ID | SP | Title | Location |
|----|-----|-------|----------|
| TD-031 | 1 | Fix AppleScript injection — strip `\n\r\x00` from project.name | `pm/terminal.py:57` |
| TD-032 | 0.5 | Add `data/`, `transcripts/`, `*.db` to `.gitignore` | `.gitignore` |
| TD-034 | 1 | Replace `datetime.utcnow()` → `datetime.now(timezone.utc)` | `pm/database/models.py` (8 sites) |
| TD-035 | 0.5 | Remove unused `ItemStatus` import | `pm/cli.py:17` |
| TD-036 | 1 | Add module docstring to `tests/test_tier1.py` | `tests/test_tier1.py` |
| TD-038 | 0.5 | Venv guard in `setup.sh` | `setup.sh` |
| TD-037 | 2 | Mock `subprocess.Popen` in terminal tests | `tests/test_terminal.py` |

### Sprint B: Dashboard UX Polish (~13 SP)

| ID | SP | Title | Location |
|----|-----|-------|----------|
| TD-023 | 1 | Reset `page=0` on filter change | `dashboard/app.py` |
| TD-025 | 1 | Make active filter labels visible | `dashboard/app.py` |
| TD-027 | 3 | Replace `st.cache_data.clear()` with session state updates (7 sites) | `dashboard/app.py` |
| TD-029 | 1 | Store `has_readme` in DB; add to health score formula | `pm/database/models.py`, `pm/scanner/detector.py` |
| TD-030 | 2 | Add `target_date`/`budget_hours`/`hours_logged` to dashboard inline edit | `dashboard/app.py` |
| TD-040 | 1 | Try/except on action buttons with `st.toast()` feedback | `dashboard/app.py` |
| TD-041 | 0.5 | Truncate Run Prompt output to 500 chars | `dashboard/app.py` |
| TD-042 | 1 | "Select All" header checkbox in batch panel | `dashboard/app.py` |
| TD-043 | 0.5 | 50KB size check before rendering docs inline | `dashboard/app.py` |
| TD-044 | 1 | Client filter dropdown in Activity tab | `dashboard/app.py` |
| TD-026 | 0.5 | Rename "St" column header; add icon legend | `dashboard/app.py` |

### Sprint C: CLI Coverage + Infrastructure (~8 SP)

| ID | SP | Title | Notes |
|----|-----|-------|-------|
| TD-024 | 3 | Batch doc generation via `run_batch_generation` with cancel | `dashboard/app.py` |
| TD-033 | 2 | Shell scripts call `pm.terminal.detect_terminal()` instead of duplicating logic | `scripts/claude-launch.sh` |
| TD-046 | 3 | `pm shutdown` support for Terminal.app | `pm/cli.py` |

---

## Future Phases (Deferred)

| Phase | Feature | Effort | Notes |
|-------|---------|--------|-------|
| 4.1 | Cross-project dependency graph | ~15 SP | Tag/client shared-library awareness |
| 3.6 | Live iMessage control interface (`pm run 3` via text) | ~10 SP | Requires stable chat.db GUID |
| — | Alembic schema versioning | ~5 SP | Replace hand-rolled `_MIGRATIONS` |
| — | REST API (`pm/api/`) | ~8 SP | Dashboard → CLI parity |
| — | Per-client billing rollup | ~3 SP | `hours_logged × rate` report |
| — | GitHub issue/PR integration | ~13 SP | Link DB records to repo issues |
| — | Export weekly client reports as PDFs | ~5 SP | |

---

## Commit Checkpoint

All work since commit `7df12fb` (2026-04-09) is **uncommitted**. Before starting Sprint A:

```bash
git add -A
git commit -m "feat: Phase 1–4 complete — agent system, memory, triage, scheduling, budget intelligence

- Phase 1 (23 SP): Security fixes, thread-safety, migration versioning, test coverage
- Phase 2 (30 SP): Refactoring, CI pipeline, DB relocation, deduplication
- Phase 3 (60+ SP): Agent assess/execute/escalate, coordinator, escalation, dashboard tab
- Phase 4 (40 SP): Agent memory, scheduling (launchd), intelligent triage, budget costs
- New commands: pm gc, pm triage, pm agent memory/costs, pm schedule
- 465 tests, 76% coverage

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
git push
```

---

## Sprint Plan

### Next: Sprint A — Quick Security + Quality Fixes (6 SP)

| # | Task | SP | File |
|---|------|----|------|
| 1 | TD-032: `.gitignore` additions | 0.5 | `.gitignore` |
| 2 | TD-035: Remove unused `ItemStatus` import | 0.5 | `pm/cli.py` |
| 3 | TD-034: `datetime.utcnow()` → timezone-aware | 1 | `pm/database/models.py` |
| 4 | TD-031: AppleScript injection strip | 1 | `pm/terminal.py` |
| 5 | TD-036: `test_tier1.py` docstring | 1 | `tests/test_tier1.py` |
| 6 | TD-037: Terminal test subprocess mocking | 2 | `tests/test_terminal.py` |
| 7 | TD-038: Venv guard in `setup.sh` | 0.5 | `setup.sh` |

Then Sprint B (dashboard UX), then Sprint C (CLI/infra).

---

## Version History

| Version | Date | Milestone |
|---------|------|-----------|
| 0.1.0 | 2026-03 | Initial: scanning, dashboard, basic CLI |
| 0.2.0 | 2026-04-09 | Code review baseline (7df12fb) |
| **0.3.0** | **2026-04-12** | **Phase 1–4 complete: full agent system** |
| 1.0.0 | TBD | Sprint A–C polish complete, stable for daily use |

---

## Key Notes

> **iMessage GUID** — The `reply` MCP tool returned "not allowlisted" for `iMessage;-;+12064962555`. Real chat_id will be captured on first live inbound message. Until then, triage/escalation iMessage sends use AppleScript directly (works), but reply polling uses chat.db GUID which may differ.

> **pm/cli.py coverage at 24%** — The CLI integration test gap is intentional: CLI commands require DB+filesystem interaction that's better covered by dedicated fixtures. The 76% overall covers all business logic modules at 80–97%.

---

## Quick Reference

| Resource | Location |
|----------|----------|
| This roadmap | `docs/ROADMAP-2026-04-12.md` |
| Code review | `code-review/2026-04-09/` |
| CI config | `.github/workflows/test.yml` |
| DB location | `~/.pm/projects.db` (override: `PM_DB_PATH`) |
| launchd (brief) | `~/Library/LaunchAgents/com.pm.daily-brief.plist` |
| launchd (agents) | `~/Library/LaunchAgents/com.pm.agent-batch.plist` |
| Dashboard | `./dashboard.sh` → http://127.0.0.1:8501 |

---

*Single source of truth as of 2026-04-12. Phase 1–4 complete with 465 tests at 76% coverage. Sprint A (quick fixes) is next priority before dashboard polish.*
